{% extends 'monTemplate.html.twig' %}

{% block sidebar %}

<body class="is-preload">
    <!-- Sidebar -->
    <section id="sidebar">
        <div class="inner">
            <nav>
                <ul>

                    <li><a href="{{ path('accueil')}}">ACCUEIL</a></li>
                </ul>
            </nav>
        </div>
    </section>

    {% endblock %}

    {% block body %}

    <div id="wrapper">
        <!-- Three -->
        <section id="intro" class="wrapper style1 fullscreen fade-up">
            <div class="inner">

                <h2>S'inscrire</h2>

                <p> Si vous ne souhaitez <strong> pas entrer d'informations personnelles</strong>, vous devez vous
                    connecter en tant qu'<strong>invité</strong> (dans la section
                    <a href="{{ path('app_login')}}">"Connexion"</a>). Cependant, un compte invité offre <strong>moins
                        de possibilité</strong> qu'un compte personnel.
                    Il vous permettra seulement de <strong>rejoindre des parties</strong> et de <strong>jouer</strong>
                    dessus.</p>

                <div class="fields">

                    {{form_start(vueFormulaireInscription) }}

                    <p> Tous les champs sont obligatoires. <br>
                        Le mot de passe doit faire au minimum 6 caractères . </p>

                    <div class="field half">
                        {{form_row(vueFormulaireInscription.email)}}
                    </div>


                    <div class="field half">
                        {{form_row(vueFormulaireInscription.pseudo)}}
                    </div>

                    <div class="field half">
                        {{form_row(vueFormulaireInscription.plainPassword)}}
                    </div>

                    <ul class="actions">
                        <li><a href="" class="button submit">Valider l'inscription </a></li>
                        <li><a href="{{ path('accueil')}}" class="button">Annuler l'inscription </a></li>
                    </ul>


                    {{form_end(vueFormulaireInscription)}}


                </div>
        </section>

        {% endblock %}